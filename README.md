# myFirstRepo
This is my first testing repositories

## Run the employee manager

1. Start the app:

```bash
python3 app.py
```

2. Open the browser at `http://localhost:8000`.

The app stores employee details in `employees.db`.
